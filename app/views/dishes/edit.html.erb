<%= render 'form' %>
<div class="text-center edit-links">
  <%= link_to "Delete Dish", dish_path(@dish), method: :delete, data: { confirm: "Are you sure you want to delete this post?" } %>
  |
  <%= link_to "cancel", dishes_path %>
</div>
